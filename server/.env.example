# Server
PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=alerting
# SSL mode options: disable, require, verify-ca, verify-full
DB_SSL_MODE=disable

# Logging
LOG_LEVEL=info